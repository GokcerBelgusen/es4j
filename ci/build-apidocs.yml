---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
- name: src

outputs:
- name: build/docs/javadoc

run:
  path: sh
  args:
    - -exc
    - |
      cd src && TERM=linux ./gradlew aggregateJavadocs
